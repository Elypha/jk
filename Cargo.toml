[package]
name = "jk"
version = "0.1.2"
edition = "2021"
description = "Transparent CLI alias runner — a small, no-magic alternative to just/make"
license = "Apache-2.0"
repository = "https://github.com/Elypha/jk"
readme = "README.md"

[lib]
name = "jk"
path = "src/lib.rs"

[[bin]]
name = "jk"
path = "src/main.rs"

[dependencies]
toml = "1.1"
serde = { version = "1", features = ["derive"] }
unicode-width = "0.2"
time = { version = "0.3", features = ["formatting", "local-offset", "macros"] }

[dev-dependencies]
tempfile = "3"

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = "z"
panic = "abort"
strip = true
